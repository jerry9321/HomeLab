apiVersion: v1
kind: Service
metadata:
  name: podinfo
  namespace: podinfo
spec:
  selector:
    app: podinfo
  ports:
    - port: 80
      targetPort: 9898
      #nodeport: 30080 # Optional, if you want to expose it on a specific NodePort from the cluster IP